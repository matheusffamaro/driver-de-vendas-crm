# URL da API (backend). Use a porta onde o backend está rodando.
# Docker (este projeto): API na porta 8000.
# Se o login falhar com CORS ou 500, confira se esta URL está correta.
NEXT_PUBLIC_API_URL=http://localhost:8000/api
